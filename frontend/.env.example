# Copy this file to .env and set your backend URL for production/mobile builds.
# In local dev, leaving this unset falls back to http://localhost:8000/api
REACT_APP_API_URL=https://your-backend-domain.com/api
